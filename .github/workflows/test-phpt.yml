---

name: test-phpt

on:
  workflow_call:
    inputs:
      build_arch:
        required: false
        type: string
        default: 'x86_64'
  workflow_dispatch:
    inputs:
      build_arch:
        type: choice
        description: Build architecture
        default: 'x86_64'
        options:
          - all
          - x86_64
          - arm64

permissions:
  contents: read

env:
  TEST_PHPT_RESULTS_SUB_DIR: _BUILT/test_phpt_results

jobs:
  setup-build-matrix:
    uses: ./.github/workflows/build-arch-matrix-generator.yml
    with:
      build_arch: ${{ inputs.build_arch }}

  test-phpt:
    name: test-phpt
    runs-on: 'ubuntu-latest'
    needs: setup-build-matrix
    timeout-minutes: 300
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.setup-build-matrix.outputs.matrix-combinations) }}
    env:
      BUILD_ARCHITECTURE: ${{ matrix.arch }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: build-native-${{ matrix.arch }}
          path: prod/native/_build/${{ matrix.arch }}-release/
      - if: ${{ matrix.goarch == 'arm64' }}
        uses: docker/setup-qemu-action@c7c53464625b32c7a7e944ae62b3e17d2b600130 # v3.7.0
        with:
          platforms: arm64
      - name: Run phpt tests
        run: |
          uname -a
          echo "Arch: ${BUILD_ARCHITECTURE}"
          source ./tools/read_properties.sh
          read_properties project.properties PROJECT_PROPERTIES
          PHP_VERSIONS=${PROJECT_PROPERTIES_SUPPORTED_PHP_VERSIONS//[()]/}
          ./tools/build/test_phpt.sh --build_architecture ${BUILD_ARCHITECTURE} --results_path "${PWD}/${{ env.TEST_PHPT_RESULTS_SUB_DIR }}" --php_versions "${PHP_VERSIONS}"
      - uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        if: failure()
        with:
          name: test-phpt-failures-${{ matrix.arch }}
          path: |
            ${{ env.TEST_PHPT_RESULTS_SUB_DIR }}/*
